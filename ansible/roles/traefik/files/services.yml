http:
  # serversTransports:
  #   insecureTransport:
  #     insecureSkipVerify: true

  routers: 
    idm:
      entryPoints:
        - "https"
      rule: "Host(`idm.{{env "IDM_DOMAIN"}}`)"
      middlewares:
        - public-with-appsec
      tls: {}
      service: lan_traefik
    service1:
      entryPoints:
        - "https"
      rule: "Host(`{{env "LAN_SERVICE1"}}.{{env "PUBLIC_DOMAIN"}}`)"
      middlewares:
        - public-with-appsec
      tls: {}
      service: lan_traefik
    service2:
      entryPoints:
        - "https"
      rule: "Host(`{{env "LAN_SERVICE2"}}.{{env "PUBLIC_DOMAIN"}}`)"
      middlewares:
        - public-with-appsec
      tls: {}
      service: lan_traefik
    

  services:
    lan_traefik:
      loadBalancer:
        servers:
          - url: "https://10.1.15.10:443"
        passHostHeader: true
        #serversTransport: insecureTransport
